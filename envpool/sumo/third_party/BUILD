cc_library(
    name = "libsumo",
    srcs = [],
    hdrs = [],
    includes = [],  # 如果有需要包含的头文件路径，添加在这里
    visibility = ["//visibility:public"],
    linkopts = ["-L$SUMO_HOME/bin", "-lsumocpp"],  # 确保路径和库名称正确
)
